[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "microfate"
authors = [{name = "TaTooKa", email = "leandro.rolando@gmail.com"}]
dynamic = ["version", "description"]
